function isInCollection(stack) {
    return achievements.isInCollection(stack);
}

function getValue(player, stack) {
    return achievements.getValue(player, stack);
}

function formatValue(player, stack, value) {
    return achievements.formatValue(player, stack, value);
}

function isObtained(player, stack) {
    return achievements.isObtained(player, stack);
}